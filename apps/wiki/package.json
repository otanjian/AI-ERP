{
  "name": "wiki",
  "version": "3.0.0",
  "private": "true",
  "dependencies": {
    "@tailwindcss/cli": "^4.0.14",
    "tailwindcss": "^4.0.14"
  },
  "devDependencies": {
    "@biomejs/biome": "1.4.1",
    "@playwright/test": "^1.49.0",
    "@tailwindcss/typography": "^0.5.16",
    "@types/node": "^22.0.0"
  },
  "scripts": {
    "postinstall": "cd frontend && yarn install",
    "dev": "cd frontend && yarn dev",
    "build": "if [ -f wiki/public/frontend/index.html ] && [ -f wiki/www/wiki.html ]; then echo 'wiki: skip vite (prebuilt assets present)'; else yarn tailwind:build && cd frontend && yarn build; fi",
    "tailwind:watch": "npx @tailwindcss/cli -i ./wiki/public/css/main.css -o ./wiki/public/css/tailwind.css --watch",
    "tailwind:build": "npx @tailwindcss/cli -i ./wiki/public/css/main.css -o ./wiki/public/css/tailwind.css",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
