/**
 * Shim for @tiptap/extension-table-cell
 * Provides default export for frappe-ui compatibility with TipTap v3
 */
import { TableCell } from '@tiptap/extension-table';
export default TableCell;
export { TableCell };
